so many things are broken right now, firstly the homepage search doesn't work,
   diffusion page and audio page is broken and they're both not full screen
   somehow its cropped into blank white on the left and right. also comparison
   doesn't seem to work as well at the leaderboard when i select models. please fix this.

I think this comes after implementing the stuff we want to create comparison between models in Leaderboard page and when we wanted to modify the database to include Audio and Diffusion models.